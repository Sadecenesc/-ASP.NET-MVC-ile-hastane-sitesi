@model IEnumerable<AcilDurumHaberi>
@{
    ViewData["Title"] = "Index";
}

<div class="container p-3">
	<div class="row pt-4">
	<div class="col-6">
			<h2 class="text-primary"> AcilDurumHaberi Listesi </h2>

	</div>
	@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
{
	<div class="col-6 text-end">
			<a asp-controller="AcilDurumHaberleri" asp-action="Create" class="btn btn-primary">
				<i class="bi bi-plus-circle"></i> &nbsp; AcilDurumHaberi OLUŞTUR
			</a>

	</div>
}
	</div>
	<br /> <br />

<table class="table table-bordered table-striped" style="width:100%"> 
	<thead>
    <tr>
		
		
	<th>

		BASLIK

	</th>
			<th>

				ACIKLAMA

			</th>
			<th>

			TARİH

			</th>
			
			
			<th></th>
		</tr>

			</thead>
	<tbody>
		@foreach(var obj in Model)
		{
			<tr>
			<td width="50%">
				@obj.Baslik
			</td>

				<td width="30%">
					@obj.Aciklama
				</td>
				<td width="30%">
					@obj.Tarih
				</td>
			
				
				<td>
						@if (User.Identity.IsAuthenticated && User.IsInRole("Admin"))
						{
							<div class="w-75 btn-group" role="group"> </div>
							<a asp asp-controller="AcilDurumHaberleri" asp-action="Edit" asp-route-id="@obj.Id" class="btn btn-primary"> <i class="bi bi-pencil-square"></i>Edit</a>
							<a asp asp-controller="AcilDurumHaberleri" asp-action="Delete" asp-route-id="@obj.Id" class="btn btn-primary"> <i class="bi bi-trash-fill"></i>Delete</a>
						}
					</td>
			</tr>


		}




	</tbody>
</table>
</div>
